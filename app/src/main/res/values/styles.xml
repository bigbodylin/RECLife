<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>


    <style name="AutoCompleteTextViewTheme" parent="Widget.AppCompat.AutoCompleteTextView">
        <!-- Customize your theme here. -->
        <item name="android:background">#ffffff</item>
        <item name="colorControlNormal">@color/wallet_holo_blue_light</item>
        <item name="colorControlActivated">#000000</item>
    </style>

    <style name="MyCustomDialog" parent="Base.Theme.AppCompat.Light.Dialog">
        <item name="android:windowNoTitle">true</item>
    </style>

    <style name="CustomTextStyle" parent="@android:style/TextAppearance.Widget.TabWidget">
        <item name="android:textSize">18dp</item>
    </style>
</resources>
